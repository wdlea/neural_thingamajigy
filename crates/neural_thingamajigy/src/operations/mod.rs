/// Defines the Exp struct which represents the Exp operation
mod exp;
/// Defines the Normalize struct which normalizes a vector passed into it
mod normalize;

pub use {exp::Exp, normalize::Normalize};
